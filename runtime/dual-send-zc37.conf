[Service]
Environment=BOOTSTRAP_PKG=harness-loader-zc37.pkg
# ponytail: drop-in overrides only the payload the NS Basic bootstrap pushes.
# Install: mkdir -p ~/.config/systemd/user/dual-send.service.d &&
#          cp runtime/dual-send-zc37.conf ~/.config/systemd/user/dual-send.service.d/ &&
#          systemctl --user daemon-reload && systemctl --user restart dual-send
# ZC37 also showed -36003 on hardware; this legacy service remains for replay only.
